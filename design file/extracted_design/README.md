
  # Finalize Proep.az Design

  This is a code bundle for Finalize Proep.az Design. The original project is available at https://www.figma.com/design/HhE0IgYigU8nQs6hLvTScf/Finalize-Proep.az-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  